import { ShowFormDirective } from './show-form.directive';

describe('ShowFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFormDirective();
    expect(directive).toBeTruthy();
  });
});
