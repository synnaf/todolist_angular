import { FlashDirective } from './flash.directive';

describe('FlashDirective', () => {
  it('should create an instance', () => {
    const directive = new FlashDirective();
    expect(directive).toBeTruthy();
  });
});
